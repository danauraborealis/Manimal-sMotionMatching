namespace Manimal.MotionMatching
{
    // Process-local diagnostic A/B switch. Runtime default includes the audited refinements.
    internal static class LocomotionRefinement
    {
        public static bool Enabled = true;
    }
}
